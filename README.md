# Mini Task Management API

## Overview
This API allows users to manage tasks with JWT authentication.

## Setup
pip install -r requirements.txt
python app.py

## Endpoints
POST /login  
GET /tasks  
POST /tasks  
GET /external-tasks  

## Example
Login:
{
  "username": "student",
  "password": "1234"
}